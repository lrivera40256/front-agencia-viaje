import { ReactNode } from "react";

export interface SocialButtonConfig {
    name: string;
    copy: string,
    icon:ReactNode,
    bgColor: string,
    textColor: string,
    borderColor: string,
}