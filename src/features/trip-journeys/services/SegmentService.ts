// Aquí conectás backend con frontend. Recibe datos crudos y los deja ricos para la UI

// export async function getSegments(tripId: string): Promise<Segment[]> {
//   const response = await fetch(`/api/trips/${tripId}/segments`);
//   return response.json();
// }